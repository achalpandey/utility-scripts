# Copy to .eval-secrets (gitignored) and fill in. Sourced by eval-stack.sh.
# Never commit the real file.

# Required — the upstream key the local gateway uses for OpenAI calls.
export OPENAI_API_KEY=sk-proj-...

# Optional — for the multi-provider phase.
export ANTHROPIC_API_KEY=
export GEMINI_API_KEY=
